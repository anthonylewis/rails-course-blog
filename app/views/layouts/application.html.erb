<!DOCTYPE html>
<html>
<head>
  <% if @post.nil? %>
    <title>Tony's Blog</title>
  <% else %>
    <title><%= @post.title %> -- Tony's Blog</title>
  <% end %>

  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
  <%= yield :head %>
</head>
<body>
  <h1>Tony's Blog</h1>

<%= yield %>

</body>
</html>
